services:
  app:
    build: .
    container_name: springboot-app
    ports:
      - "8080:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - /home/mai-van-nam/app-config/.env
    restart: always
